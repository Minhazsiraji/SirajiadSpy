'use client';import{createClient}from'@supabase/supabase-js';let instance:ReturnType<typeof createClient>|null=null;export function browserSupabase(){if(instance)return instance;const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!url||!key)throw new Error('Supabase browser auth is not configured');instance=createClient(url,key);return instance}export async function authFetch(input:RequestInfo|URL,init:RequestInit={}){const{data}=await browserSupabase().auth.getSession(),headers=new Headers(init.headers);if(data.session?.access_token)headers.set('authorization',`Bearer ${data.session.access_token}`);return fetch(input,{...init,headers})}
