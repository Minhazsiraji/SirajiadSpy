import{lookup}from'dns/promises';import{isIP}from'net';
function privateIp(ip:string){const v=ip.replace(/^\[|\]$/g,'').toLowerCase();if(v==='::1'||v==='0:0:0:0:0:0:0:1'||v.startsWith('fc')||v.startsWith('fd')||v.startsWith('fe80:'))return true;if(!isIP(v))return false;if(v.startsWith('127.')||v.startsWith('10.')||v.startsWith('192.168.')||v.startsWith('169.254.')||v.startsWith('0.'))return true;const m=v.match(/^172\.(\d+)\./);return Boolean(m&&Number(m[1])>=16&&Number(m[1])<=31)}
export async function validateOutboundUrl(value:string){const url=new URL(value);if(!['http:','https:'].includes(url.protocol))throw new Error('Only HTTP(S) URLs are supported');if(url.username||url.password)throw new Error('Credentialed URLs are not supported');const host=url.hostname.toLowerCase();if(host==='localhost'||host.endsWith('.local')||host.endsWith('.internal')||host==='metadata.google.internal')throw new Error('Private or local addresses are not supported');const addresses=await lookup(host,{all:true,verbatim:true});if(!addresses.length||addresses.some(x=>privateIp(x.address)))throw new Error('Private or unresolved network destination blocked');return url}
export async function safeFetch(value:string,init:RequestInit={},maxRedirects=4):Promise<Response>{let current=(await validateOutboundUrl(value)).toString();for(let i=0;i<=maxRedirects;i++){const response=await fetch(current,{...init,redirect:'manual'});if(response.status>=300&&response.status<400){const location=response.headers.get('location');if(!location)throw new Error('Redirect without location blocked');current=(await validateOutboundUrl(new URL(location,current).toString())).toString();continue}return response}throw new Error('Too many redirects')}
