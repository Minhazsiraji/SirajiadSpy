import type{LandingAnalysis}from'./types';import{supabaseAdmin}from'./repository';
function safeUrl(value:string){const url=new URL(value);if(!['http:','https:'].includes(url.protocol))throw new Error('Only HTTP(S) landing pages are supported');if(['localhost','127.0.0.1','::1'].includes(url.hostname))throw new Error('Local addresses are not supported');return url.toString()}
async function screenshot(url:string){const token=process.env.BROWSERLESS_TOKEN;if(!token)throw new Error('Browserless is required for landing screenshots');const response=await fetch(`https://production-sfo.browserless.io/screenshot?token=${encodeURIComponent(token)}`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({url,options:{fullPage:false,type:'jpeg',quality:78}}),signal:AbortSignal.timeout(90000)});if(!response.ok)throw new Error(`Screenshot failed (${response.status})`);return Buffer.from(await response.arrayBuffer())}
async function upload(buffer:Buffer,id:string){const db=supabaseAdmin(),bucket='landing-screenshots';const{data:buckets}=await db.storage.listBuckets();if(!buckets?.some(b=>b.name===bucket))await db.storage.createBucket(bucket,{public:true,fileSizeLimit:8*1024*1024});const path=`${id}/${Date.now()}.jpg`;const{error}=await db.storage.from(bucket).upload(path,buffer,{contentType:'image/jpeg',upsert:true});if(error)throw new Error(`Screenshot upload failed: ${error.message}`);return db.storage.from(bucket).getPublicUrl(path).data.publicUrl}
export async function analyzeLandingPage(urlValue:string,id='ad'):Promise<LandingAnalysis>{const url=safeUrl(urlValue),start=Date.now();const[htmlResult,image]=await Promise.all([fetch(url,{headers:{'user-agent':'Mozilla/5.0'},redirect:'follow',signal:AbortSignal.timeout(25000)}).then(r=>r.text()),screenshot(url)]);const elapsed=Date.now()-start,html=htmlResult.toLowerCase();let isShopify=/shopify|cdn\.shopify\.com|window\.shopify/.test(html);if(!isShopify)try{const product=new URL('/products.json',url);const r=await fetch(product,{signal:AbortSignal.timeout(8000)});isShopify=r.ok&&r.headers.get('content-type')?.includes('json')===true}catch{}const screenshotUrl=await upload(image,id);return{screenshotUrl,isShopify,platform:isShopify?'Shopify':/woocommerce|wp-content/.test(html)?'WooCommerce':'Custom / other',pageSpeed:elapsed,hasCOD:/cash on delivery|\bcod\b|ক্যাশ অন ডেলিভারি/.test(html),hasBkash:/bkash|বিকাশ/.test(html),hasNagad:/nagad|নগদ/.test(html),analyzedAt:new Date().toISOString()}}
