import{NextRequest,NextResponse}from'next/server';import{findAds}from'@/lib/repository';import{normalizeCountry}from'@/lib/countries';
export async function GET(req:NextRequest){try{const s=req.nextUrl.searchParams,country=normalizeCountry(s.get('country')),result=await findAds({q:s.get('q')||undefined,country,minDays:Number(s.get('minDays')||0),mediaType:s.get('mediaType')||'ALL',sortBy:s.get('sortBy')||'signalScore',page:Number(s.get('page')||1),limit:Number(s.get('limit')||30)});return NextResponse.json({...result,country,source:'database'})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Database query failed'},{status:503})}}
