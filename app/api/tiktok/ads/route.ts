import{NextResponse}from'next/server';import{findTikTokAds}from'@/lib/repository';import{normalizeCountry}from'@/lib/countries';
export async function GET(request:Request){try{const u=new URL(request.url),q=u.searchParams.get('q')||'',country=normalizeCountry(u.searchParams.get('country')),ads=await findTikTokAds(q,30,country);return NextResponse.json({ads,country,isMock:ads.length>0&&ads.every(a=>a.isMock)})}catch(e){return NextResponse.json({error:'Could not load TikTok evidence',detail:e instanceof Error?e.message:'Unknown error'},{status:500})}}
