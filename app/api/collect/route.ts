import{NextRequest,NextResponse}from'next/server';import{z}from'zod';import{collectAds}from'@/lib/meta-collector';import{authorizeUsage,accessResponse}from'@/lib/auth-server';const Body=z.object({query:z.string().trim().min(1).max(100),country:z.string().length(2).default('BD')});export async function POST(req:NextRequest){try{const usage=await authorizeUsage(req,'search'),body=Body.parse(await req.json()),result=await collectAds(body.query,body.country);return NextResponse.json({...result,usage})}catch(e){return accessResponse(e)||NextResponse.json({error:'Invalid collection request',detail:e instanceof Error?e.message:'Unknown error'},{status:400})}}
