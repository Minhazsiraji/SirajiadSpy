'use client';import Link from'next/link';import{useAuth}from'./AuthProvider';import{browserSupabase}from'@/lib/supabase-browser';export function AccountDock(){const{session,loading}=useAuth();if(loading)return null;return <div className="account-dock">{session?<><span>{session.user.email}</span><button onClick={()=>browserSupabase().auth.signOut()}>Sign out</button></>:<Link href="/login">Sign in</Link>}</div>}
