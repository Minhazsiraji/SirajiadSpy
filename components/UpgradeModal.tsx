'use client';import{UPGRADE}from'@/lib/plans';
export function UpgradeModal({open,onClose}:{open:boolean;onClose:()=>void}){if(!open)return null;const billingUrl=process.env.NEXT_PUBLIC_AGENTSIRAJI_BILLING_URL||'https://agentsiraji.com';return <div className="modal-backdrop" onMouseDown={onClose}><section className="upgrade-modal" role="dialog" aria-modal="true" aria-labelledby="upgrade-title" onMouseDown={e=>e.stopPropagation()}><button className="modal-close" onClick={onClose} aria-label="Close">×</button><span className="kicker">SIRAJI ADSPY PRO</span><h2 id="upgrade-title">Unlock your paid growth toolkit</h2><p>AdSpy subscriptions are sold and managed through AgentSiraji. Your AdSpy access follows the plan entitlement on your AgentSiraji account.</p><div className="price"><b>Pro access</b><span> · managed by AgentSiraji</span></div><a className="whatsapp" href={billingUrl}>View plans / manage subscription →</a><small style={{display:'block',marginTop:12,opacity:.7}}>No payment information is collected inside Siraji AdSpy.</small></section></div>}
